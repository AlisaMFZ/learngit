'use strict';

(function (exports) {
    var postProcessor = function postProcessor() {};

    postProcessor.prototype = {
        process: function process(css) {
            return 'hr {height:50px;}\n' + css;
        }
    };

    exports.install = function (less, pluginManager) {
        pluginManager.addPostProcessor(new postProcessor());
    };
})(typeof exports === 'undefined' ? undefined['postProcessorPlugin'] = {} : exports);

//# sourceMappingURL=index-compiled.js.map